{
  "name": "gist-uploader-action",
  "version": "0.1.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "biome format --write",
    "lint": "biome lint --write",
    "check": "biome check"
  },
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "2.2.7",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
