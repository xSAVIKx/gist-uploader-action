{
  "name": "gist-uploader-action",
  "version": "0.1.1",
  "description": "Performs uploads of repo files to gists\n\n",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "biome format --write",
    "lint": "biome lint --write",
    "check": "biome check"
  },
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "2.2.7",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@actions/core": "1.11.1",
    "@actions/github": "6.0.1"
  }
}
